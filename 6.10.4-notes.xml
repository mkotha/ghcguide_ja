<?xml version="1.0" encoding="iso-8859-1"?>
<sect1 id="release-6-10-4">
  <title>Release notes for version 6.10.4</title>

  <para>
    6.10.4 is a bugfix release over 6.10.3. Very few changes have been
    made. They are listed below.
  </para>

  <itemizedlist>
    <listitem>
      <para>
        Fixed a segfault that could occur when calling
        <literal>mkWeakForeignEnv#</literal>.
      </para>
    </listitem>

    <listitem>
      <para>
        Fixed a crash that could occur when calling
        <literal>unblock</literal> inside <literal>unsafePerformIO</literal>.
      </para>
    </listitem>

    <listitem>
      <para>
        Stop primops such as <literal>readMutVar#</literal> being
        inlined, which can cause loss of referential transparency.
      </para>
    </listitem>

    <listitem>
      <para>
        Fixed a bug that meant ctrl-C was ignored by processes we
        run with <literal>runProcess</literal>.
      </para>
    </listitem>

    <listitem>
      <para>
        Fixed a bug where gcc processes run by GHC get killed on
        Mac OS X 10.4.11.
      </para>
    </listitem>

    <listitem>
      <para>
        Fixed a bug where processes on Windows could hold file handles
        open after they had finished, leading to permission denied errors.
      </para>
    </listitem>

    <listitem>
      <para>
        GHCi only uses Haskeline if the input is a terminal.
      </para>
    </listitem>

    <listitem>
      <para>
        Fixed building with newer versions of MSYS binutils.
      </para>
    </listitem>
  </itemizedlist>
</sect1>

