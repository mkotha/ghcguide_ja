<?xml version="1.0" encoding="iso-8859-1"?>
<sect1 id="release-7-0-3">
  <title>Release notes for version 7.0.3</title>

  <para>
    The significant changes to the various parts of the compiler
    since 7.0.2 are listed below.
  </para>

  <itemizedlist>
    <listitem>
      <para>
        GHC on OS X now works with XCode 4.
      </para>
    </listitem>

    <listitem>
      <para>
        Object splitting (which gives smaller binaries) is now enabled
        on OS X provided you have XCode 3.2 or later.
      </para>
    </listitem>

    <listitem>
      <para>
        A compiler panic when compiling large files with LLVM has been
        fixed.
      </para>
    </listitem>

    <listitem>
      <para>
        Some bad floating point results on x86 have been fixed.
      </para>
    </listitem>

    <listitem>
      <para>
        The <literal>-read_only_relocs</literal> flag is no longer used
        on OS X 64, which eliminates some warnings.
      </para>
    </listitem>

    <listitem>
      <para>
        Some section misnumberings in the documentation have been corrected.
      </para>
    </listitem>

    <listitem>
      <para>
        The documentation is installed into the correct location by
        the Windows installer.
      </para>
    </listitem>

    <listitem>
      <para>
        The documentation is now included in the OS X installer.
      </para>
    </listitem>

    <listitem>
      <para>
        Some small documentation tweaks have been made.
      </para>
    </listitem>

    <listitem>
      <para>
        Some duplicate files have been removed from the bindists.
      </para>
    </listitem>
  </itemizedlist>

</sect1>

